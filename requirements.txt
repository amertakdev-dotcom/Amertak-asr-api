# ─── Web Framework ───────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ─── ASR Core (original dependencies from Infinity-Khmer-ASR) ─
transformers==4.47.1
torchaudio==2.5.1
sentencepiece==0.2.0
accelerate==1.2.1

# ─── PyTorch CPU (Render Free tier — CPU only) ────────────────
# Install torch CPU separately via --index-url (see README)
# torch==2.5.1+cpu   ← installed via pip extra-index

# ─── Audio processing ─────────────────────────────────────────
pydub==0.25.1

# ─── Hugging Face Hub (model download) ───────────────────────
huggingface_hub==0.27.1

# ─── Utility ──────────────────────────────────────────────────
numpy==1.26.4
